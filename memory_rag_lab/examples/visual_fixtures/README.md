# Visual Fixtures

This folder contains minimal offline fixtures for vision integration demos.

- `*.jpg`: placeholder image files used only for path-based analysis in offline mode
- `*.vision.json`: sidecar analysis payloads consumed by `memory_rag_lab.vision`
- `fixture_manifest.json`: one-file index for scripted validation

These fixtures intentionally avoid external APIs so demos are reproducible.
