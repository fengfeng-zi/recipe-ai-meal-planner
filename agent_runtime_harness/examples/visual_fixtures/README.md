# Visual Fixtures

This folder contains minimal offline fixtures for runtime image-analysis demos.

- `*.jpg`: placeholder image files for deterministic local runs
- `*.vision.json`: sidecar metadata read by `agent_runtime_harness.vision`
- `fixture_manifest.json`: fixture index for smoke tests

No network access is required to validate these fixtures.
